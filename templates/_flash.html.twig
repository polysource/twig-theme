{% if app.session is defined and app.session.flashbag is defined %}
    {% for type, messages in app.session.flashbag.all %}
        {% for message in messages %}
            {% set bsClass = {
                'success': 'success',
                'error': 'danger',
                'warning': 'warning',
                'notice': 'info',
            }[type]|default('secondary') %}
            <div class="alert alert-{{ bsClass }} alert-dismissible fade show" role="alert">
                {# Defensive `|e` even though Twig auto-escape is on by
                 # default — flash messages may include strings derived
                 # from exception text, so make the escape explicit. #}
                {{ message|e }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
    {% endfor %}
{% endif %}
